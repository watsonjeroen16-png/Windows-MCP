# Kaizi app environment. Copy to .env (or export in your shell) before `npm start`.
# Leave unset to run fully offline against the built-in mock API
# (verification code 000000 is accepted in that mode).
EXPO_PUBLIC_API_URL=http://localhost:4000
